# دليل رفع المشروع على cPanel - خطوة بخطوة

## 📚 المتطلبات
- حساب cPanel بدعم Node.js
- FTP Client (FileZilla أو cPanel File Manager)
- قاعدة بيانات MySQL متاحة

---

## 🗄️ الجزء الأول: إعداد قاعدة البيانات MySQL

### الخطوة 1: إنشاء قاعدة البيانات
1. سجّل دخول إلى cPanel
2. ابحث عن **MySQL® Databases**
3. في قسم "Create New Database":
   - أدخل اسم: `ali_termin`
   - اضغط **Create Database**
4. سجّل الاسم الكامل (سيكون بصيغة: `cpanelusername_ali_termin`)

### الخطوة 2: إنشاء مستخدم قاعدة البيانات
1. في نفس الصفحة، قسم "Add New User":
   - Username: `ali_admin`
   - Password: أنشئ كلمة مرور قوية (احفظها!)
   - اضغط **Create User**
2. سجّل اسم المستخدم الكامل (سيكون: `cpanelusername_ali_admin`)

### الخطوة 3: ربط المستخدم بقاعدة البيانات
1. في قسم "Add User To Database":
   - اختر المستخدم: `cpanelusername_ali_admin`
   - اختر القاعدة: `cpanelusername_ali_termin`
   - اضغط **Add**
2. في صفحة الصلاحيات، اختر **ALL PRIVILEGES**
3. اضغط **Make Changes**

### الخطوة 4: استيراد Schema
1. افتح **phpMyAdmin** من cPanel
2. اختر قاعدة البيانات `cpanelusername_ali_termin` من القائمة اليسرى
3. اضغط على تبويب **Import** في الأعلى
4. اضغط **Choose File** وارفع ملف `backend/sql/schema.sql`
5. اضغط **Go** في الأسفل
6. تأكد من ظهور رسالة نجاح وإنشاء الجداول (users, appointments, message_logs)

---

## 🔧 الجزء الثاني: رفع الـ Backend

### الخطوة 5: تحضير ملفات Backend على جهازك

1. **افتح ملف `backend/.env` وعدّل القيم:**
   ```env
   # Database Configuration
   DB_HOST=localhost
   DB_USER=cpanelusername_ali_admin
   DB_PASSWORD=your_database_password_here
   DB_NAME=cpanelusername_ali_termin
   
   # JWT Configuration
   JWT_SECRET=your-super-secret-key-change-this-in-production
   JWT_EXPIRE=12h
   
   # Server Configuration
   PORT=5000
   NODE_ENV=production
   
   # Twilio Configuration (اتركها كما هي إذا لم تحصل على حساب Twilio بعد)
   TWILIO_ACCOUNT_SID=your_twilio_account_sid
   TWILIO_AUTH_TOKEN=your_twilio_auth_token
   TWILIO_SMS_FROM=+1234567890
   TWILIO_WHATSAPP_FROM=whatsapp:+1234567890
   MOCK_MESSAGING=true
   
   # Application Configuration
   APP_TIMEZONE=Europe/Zurich
   PUBLIC_BASE_URL=https://yourdomain.com
   ```

2. **احذف مجلد `node_modules` من Backend إذا كان موجودًا** (لتقليل حجم الرفع)

### الخطوة 6: رفع ملفات Backend إلى cPanel

**الطريقة الأولى: باستخدام File Manager في cPanel (الأسهل)**
1. افتح **File Manager** في cPanel
2. اذهب إلى مجلد `home` (المجلد الرئيسي)
3. أنشئ مجلد جديد اسمه `ali-backend`
4. ادخل إلى المجلد
5. اضغط **Upload** 
6. ارفع جميع ملفات Backend كملف ZIP:
   - اضغط بزر الماوس الأيمن على مجلد `backend` → Compress → ZIP
   - ارفع الملف المضغوط
   - بعد الرفع، اختر الملف واضغط **Extract**

**الطريقة الثانية: باستخدام FileZilla (FTP)**
1. حمّل FileZilla من https://filezilla-project.org
2. افتح FileZilla وأدخل بيانات FTP من cPanel:
   - Host: `ftp.yourdomain.com` أو `yourdomain.com`
   - Username: اسم مستخدم cPanel
   - Password: كلمة مرور cPanel
   - Port: 21
3. اتصل بالسيرفر
4. في الجانب الأيمن (Remote), اذهب للمجلد الرئيسي
5. أنشئ مجلد `ali-backend`
6. في الجانب الأيسر (Local), افتح مجلد `backend`
7. اسحب جميع الملفات من اليسار لليمين (بدون مجلد node_modules)

### الخطوة 7: تثبيت Dependencies على السيرفر

**إذا كان cPanel يدعم Node.js App:**
1. في cPanel، ابحث عن **Setup Node.js App** أو **Node.js Selector**
2. اضغط **Create Application**
3. املأ البيانات:
   - Node.js version: اختر أحدث إصدار متاح (18.x أو أعلى)
   - Application mode: **Production**
   - Application root: `/home/cpanelusername/ali-backend`
   - Application URL: اختر subdomain أو path مثل `api.yourdomain.com`
   - Application startup file: `src/server.js`
4. اضغط **Create**
5. بعد إنشاء التطبيق، اضغط **Run NPM Install** لتثبيت الحزم
6. اضغط **Restart** لتشغيل التطبيق

**إذا كان cPanel يستخدم Terminal/SSH:**
1. افتح **Terminal** في cPanel
2. نفذ الأوامر:
   ```bash
   cd ~/ali-backend
   npm install --production
   node src/server.js
   ```

### الخطوة 8: التأكد من عمل Backend

1. افتح المتصفح واذهب إلى: `https://api.yourdomain.com/health` أو `https://yourdomain.com:5000/health`
2. يجب أن ترى:
   ```json
   {"status":"ok","timestamp":"2026-02-25T..."}
   ```
3. إذا لم يعمل، افتح **Error Log** في cPanel Node.js App وتحقق من الأخطاء

---

## 🎨 الجزء الثالث: بناء ورفع Frontend

### الخطوة 9: بناء Frontend على جهازك

1. **افتح ملف `frontend/.env` أو أنشئه:**
   ```env
   VITE_API_BASE_URL=https://api.yourdomain.com
   ```
   غيّر `api.yourdomain.com` إلى عنوان الـ Backend الفعلي

2. **افتح Terminal في مجلد frontend:**
   ```bash
   cd frontend
   npm install
   npm run build
   ```

3. سيتم إنشاء مجلد **`dist`** بداخله جميع الملفات الثابتة

### الخطوة 10: رفع ملفات Frontend

**باستخدام File Manager:**
1. افتح **File Manager** في cPanel
2. اذهب إلى `public_html` (هذا هو المجلد الذي يظهر على yourdomain.com)
3. احذف أي ملفات قديمة (احتفظ بنسخة احتياطية إذا كان موقع موجود)
4. ارفع محتويات مجلد **`frontend/dist`** (ليس المجلد نفسه، بل الملفات بداخله):
   - `index.html`
   - مجلد `assets`
   - أي ملفات أخرى

**باستخدام FileZilla:**
1. اتصل بالسيرفر عبر FTP
2. في الجانب الأيمن, اذهب إلى `public_html`
3. في الجانب الأيسر, افتح `frontend/dist`
4. اسحب جميع الملفات من dist إلى public_html

### الخطوة 11: إعداد .htaccess للـ React Router

1. في `public_html`, أنشئ أو عدّل ملف `.htaccess`:
   ```apache
   <IfModule mod_rewrite.c>
     RewriteEngine On
     RewriteBase /
     RewriteRule ^index\.html$ - [L]
     RewriteCond %{REQUEST_FILENAME} !-f
     RewriteCond %{REQUEST_FILENAME} !-d
     RewriteRule . /index.html [L]
   </IfModule>
   ```

2. هذا يضمن أن React Router يعمل بشكل صحيح على الروابط المباشرة

---

## 🧪 الجزء الرابع: اختبار المشروع

### الخطوة 12: اختبار كامل للنظام

1. **افتح الموقع:** `https://yourdomain.com`
2. **سجّل مستخدم جديد:**
   - اذهب لصفحة التسجيل
   - أدخل البيانات
   - تأكد من إنشاء الحساب بنجاح
3. **سجّل دخول:**
   - استخدم البيانات التي أنشأتها
   - تأكد من الوصول للـ Dashboard
4. **اختبر الميزات:**
   - أنشئ موعد جديد
   - جرّب تصدير Excel (يجب أن يعمل الآن بعد الإصلاح الأخير)
   - افتح صفحة Messages
   - افتح صفحة Settings

### الخطوة 13: حل المشاكل الشائعة

**مشكلة: Backend لا يعمل**
- تحقق من Error Log في Node.js App
- تأكد من صحة بيانات `.env` خاصة DB_HOST, DB_USER, DB_PASSWORD, DB_NAME
- تأكد من تثبيت dependencies بـ `npm install`

**مشكلة: Frontend يعمل لكن API لا يستجيب**
- تحقق من `VITE_API_BASE_URL` في ملف `.env` قبل البناء
- تأكد من عدم حظر CORS على Backend
- افتح Console في المتصفح (F12) وشاهد أخطاء الشبكة

**مشكلة: قاعدة البيانات لا تتصل**
- تأكد من استخدام `localhost` كـ DB_HOST
- تأكد من صحة اسم المستخدم والقاعدة (مع prefix اسم مستخدم cPanel)
- جرّب الاتصال من phpMyAdmin للتأكد من الصلاحيات

**مشكلة: الروابط لا تعمل في Frontend (404)**
- تأكد من وجود ملف `.htaccess` في `public_html`
- تأكد من تفعيل `mod_rewrite` في Apache (عادة مفعّل افتراضيًا)

---

## 🔐 الجزء الخامس: إعدادات الأمان

### الخطوة 14: تأمين المشروع

1. **غيّر JWT_SECRET في `.env`:**
   - استخدم سلسلة عشوائية قوية طويلة
   - يمكنك توليدها من: https://randomkeygen.com/

2. **فعّل HTTPS:**
   - في cPanel, اذهب إلى **SSL/TLS Status**
   - فعّل SSL لدومينك (Let's Encrypt مجاني)
   - تأكد من فتح الموقع بـ `https://`

3. **قيّد الوصول لـ .env:**
   - في File Manager, اختر ملف `.env`
   - اضغط **Permissions**
   - اجعلها `600` (read/write للمالك فقط)

4. **احذف ملفات غير ضرورية:**
   - احذف `node_modules` من أي مكان إذا رفعته بالخطأ
   - احذف ملفات `.git` إذا كانت موجودة

---

## 🚀 الجزء السادس: إعداد Twilio (اختياري)

### الخطوة 15: تفعيل إرسال الرسائل

1. **أنشئ حساب Twilio:**
   - اذهب إلى https://www.twilio.com/try-twilio
   - سجّل حساب مجاني (يعطيك رصيد تجريبي)

2. **احصل على المعلومات:**
   - من Dashboard, انسخ:
     - Account SID
     - Auth Token
   - اشتر رقم هاتف من **Phone Numbers** → **Buy a Number**
   - لـ WhatsApp: اذهب لـ **Messaging** → **Try it out** → **Send a WhatsApp message**

3. **عدّل `.env` على السيرفر:**
   ```env
   TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxx
   TWILIO_AUTH_TOKEN=your_actual_auth_token
   TWILIO_SMS_FROM=+41123456789
   TWILIO_WHATSAPP_FROM=whatsapp:+41123456789
   MOCK_MESSAGING=false
   ```

4. **أعد تشغيل Backend:**
   - من Node.js App في cPanel, اضغط **Restart**

---

## ✅ الخطوة النهائية: التأكد من كل شيء

- [ ] قاعدة البيانات تعمل وفيها الجداول
- [ ] Backend يستجيب على `/health`
- [ ] Frontend يفتح على الدومين الرئيسي
- [ ] يمكن التسجيل وتسجيل الدخول
- [ ] يمكن إنشاء وعرض المواعيد
- [ ] تصدير Excel يعمل بدون "Unauthorized"
- [ ] صفحة Settings تفتح
- [ ] HTTPS مفعّل

---

## 📞 الدعم

إذا واجهت أي مشكلة:
1. افتح **Error Log** في cPanel Node.js App
2. افتح Browser Console (F12) → **Console** tab
3. تحقق من phpMyAdmin أن الجداول موجودة
4. تأكد من تطابق الإصدارات (Node.js 18+)

---

## 🔄 تحديثات مستقبلية

**لتحديث Backend:**
1. عدّل الملفات محليًا
2. ارفع الملفات المعدّلة عبر FTP أو File Manager (استبدل القديمة)
3. إذا غيّرت package.json, نفّذ `npm install` على السيرفر
4. أعد تشغيل Backend من cPanel

**لتحديث Frontend:**
1. عدّل الملفات محليًا
2. نفّذ `npm run build` في مجلد frontend
3. ارفع محتويات `dist` الجديدة إلى `public_html` (استبدل القديم)
4. امسح Cache المتصفح (Ctrl+Shift+R)

---

**تم بحمد الله! 🎉**
المشروع الآن يعمل على cPanel بشكل كامل.
